# catChat Incoming Webhook URL
# Create a webhook via POST /api/webhooks and paste the returned URL here.
CATCHAT_WEBHOOK_URL=http://localhost:8100/api/webhooks/1/your-token-here

# Optional prefix added to every message (e.g. "[PROD]" or "[CI]")
MESSAGE_PREFIX=
